{
  "name": "push-my-way-db",
  "private": true,
  "scripts": {
    "migrate": "dotenv -e ../.env -- supabase db push"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.81.1",
    "dotenv": "^17.2.3",
    "dotenv-cli": "^11.0.0"
  }
}
