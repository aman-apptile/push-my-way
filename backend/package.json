{
  "name": "push-my-way-backend",
  "scripts": {
    "dev": "dotenv -e ../.env -- bun run --hot src/index.ts",
    "build": "bun build src/index.ts --outdir dist",
    "start": "dotenv -e ../.env -- bun run dist/index.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.81.1",
    "dotenv": "^17.2.3",
    "dotenv-cli": "^11.0.0",
    "hono": "^4.10.6"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
