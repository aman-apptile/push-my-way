{
  "name": "codepush-my-way",
  "private": true,
  "scripts": {
    "dev:backend": "cd backend && dotenv -e ../.env -- bun run dev",
    "build:backend": "cd backend && bun run build",
    "start:backend": "cd backend && dotenv -e ../.env -- bun run start",
    "dev:mobile": "cd app && bun run start",
    "android": "cd app && bun run android",
    "ios": "cd app && bun run ios",
    "db:migrate": "cd db && dotenv -e ../.env -- bun run migrate"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "dotenv-cli": "^11.0.0"
  }
}
